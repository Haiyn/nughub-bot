import { Service } from '@services/service';
import { injectable } from 'inversify';

/** A service that handles Discord roles and their permissions */
@injectable()
export class PermissionService extends Service {}
